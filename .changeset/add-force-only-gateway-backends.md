---
"@pedro_klein/pi-gateway": minor
---

Add force-only backends that are excluded from automatic fallback routing and become eligible only while explicitly selected with `/gateway force <backend>`.
