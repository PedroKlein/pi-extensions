{
  "name": "@pedroklein/pi-task",
  "version": "0.1.0",
  "description": "Task graph manager for Pi — DAG-based plans with sub-tasks and parallel groups",
  "type": "module",
  "main": "./index.ts",
  "files": ["dist", "*.ts", "lib/"],
  "scripts": {
    "build": "tsup index.ts --format esm --dts",
    "clean": "rm -rf dist"
  },
  "pi": {
    "extensions": ["./index.ts"]
  },
  "keywords": ["pi-package", "pi", "task", "planning", "dag", "coding-agent"],
  "license": "MIT",
  "author": "Pedro Klein",
  "engines": { "node": ">=20.0.0" },
  "peerDependencies": {
    "@earendil-works/pi-coding-agent": "*",
    "@earendil-works/pi-tui": "*"
  },
  "devDependencies": {
    "tsup": "^8.0.0",
    "typescript": "^5.7.0"
  }
}
